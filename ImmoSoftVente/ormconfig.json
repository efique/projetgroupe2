{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "",
  "database": "immosoftvente",
  "synchronize": false,
  "dropSchema": false,
  "logging": false,
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "default": true,
  "cli": {
    "migrationsDir": "src/migration"
  }
}